# 260921 IDispatch 桥接修复（EXE 工程类过 COM 边界）

接续文档：跨会话恢复上下文的第一锚点。修复对象 = VBMAN_DEMO.exe 启动崩溃 0xC0000005。

## 崩溃机制（三路证据互洽，勿重查）

- demo 侧 `New bHello`（EXE 工程类，MultiUse）生成裸 C 结构体指针，塞进 VT_DISPATCH VARIANT 传给 VBMAN.dll
- DLL 侧 `Set Controllers(ControllerName) = Controller`（cHttpServerRouter.cls:63，Dictionary 是 VBMAN 工程类）取值/释放路径对 VT_DISPATCH AddRef → 裸结构体首字段被当 vtable → `callq *0x8(%rax)` → AV
- 接收侧 `cHttpServerRouter.cls:236-237` 用 `CallByName C, ActionName, VbMethod, ctx` 晚绑定 —— 需要真 IDispatch 应答 GetIDsOfNames + Invoke
- 本质：C3 的 IDispatch 生成原先被 8 处 `isDll_` 守卫锁在 ActiveX DLL 侧；EXE 工程类实例无 `__comObj`、无 coclass 表、vb6comserver*.c 不链入
- 方向已拍板（用户）：完整实现 IDispatch 桥接（非报错拦截）；tests 布局一起统一到 tests_github

## 四阶段进度

| 阶段 | 状态 | 内容 |
|---|---|---|
| 1 | ✅ 代码就绪（未提交未验证） | 打通结构体与链入 |
| 2 | ✅ 被阶段 1 覆盖 | tables.inc 的方法表 + invoke 桥接在 `includeDllExports` 守卫外生成，com_entry.c 自动获得完整 IDispatch 方法表，无需单独实施 |
| 3 | ⬜ 未开始 | 打包点接入 —— 真正止血的一步 |
| 4 | ⬜ | tests_github 用例 + ci_vbman.yml 路径统一 + 全量回归 |

## 阶段 1 改动清单（7 文件，96+/16-，注释前缀 ExeComBridge 01）

1. `src/backend/detail/base/cgen_base_generate_c_open.inc` — `__comObj` 字段无条件生成（:62-72）；vb6comserver.h include 条件 `isDll_ && isClassModule_` → `isClassModule_`（:25-31，防字段访问器 C4013 截断指针）
2. `src/backend/module/cgen_com.cpp` — `_New()` 无条件初始化 `__comObj = NULL`（:22-26）；`emitClassFieldAccessors` 放开 EXE（:166-175）
3. `src/backend/detail/util/cgen_api.inc:71-79` + `src/backend/cgen_util.cpp:32` — `generateDllEntry` 第三参 `bool includeDllExports = true`（DLL 调用点零改动）
4. `src/backend/detail/util/cgen_util_dllentry_exports.inc` — `g_vb6_coclassCount` 留 if 外；导出函数段（含 DllMain）包进 `if (includeDllExports)`
5. `src/driver/detail/driver_codegen_dll_entry.inc:22-70` — EXE 平行分支生成 com_entry.c；progId 链：projectBaseName_ → options.dllProgId → "VB6EXE"
6. `src/driver/driver_link.cpp:169-185` — vb6comserver 5 个 .c 无条件链入；com_entry.c 以 `!msvcOpts.isDll` 链入（DLL 不生成该文件，无条件加会 C1083）

已核实结论（勿重查）：`emitClassFactory`/`emitClassFieldAccessors` 调用点在 `isClassModule_ && !currentClassIsInterface` 守卫下、无 isDll_（body_pass.inc:22-27）；ole32+oleaut32+uuid+advapi32 六链接分支齐全（msvc_driver.cpp:229/242/255 + incremental:273/278/283）；vb6comserver 对生成符号零直接引用，唯一跨单元依赖 g_vb6_coclasses/Count 由 com_entry.c（EXE）/dll_entry.c（DLL）提供；纯 EXE 下 `__comObj` 恒 NULL 无读方，行为零变化。

## 阶段 3 待办（打包点接入）

1. `src/backend/expr/cgen_expr.cpp:267-289` 附近 NewExpr 分支：`New bHello` 现生成裸 `(vb6_cls_bHello_New())`；需在 As Object/Variant 参数上下文（ParameterInfo）改为经 `vb6_ComObject_FromInstance` 包装
2. `src/backend/cgen_util_com.cpp:194-269` comPackExpr 补工程类分支；或 `src/rtl/core/vb6com/vb6com_pack.c:57-79` vb6_ComPackObject 对工程类实例走 FromInstance（参照 vb6_Host_IsHostObject 白名单先例，uc_hostmodel.c:68-81）
3. `src/backend/cgen_util_type.cpp` inferExprType 无 NewExpr case 返回 Variant 的问题
4. **收窄原则**：按 ParameterInfo 上下文收窄，泛用实参包装改动曾致回归 30→81（HANDOFF 教训）
5. 禁区仍有效：不动 FireEvent 广播（cgen_com_events.cpp:67 的 isDll_ 守卫，EXE 无此需求）

## 阶段 4 待办（验证与布局统一）

1. tests_github 新增最小复现用例：EXE 工程类实例传 As Object 参数 + 对端 CallByName（先失败后通过）
2. `ci_vbman.yml` 的 tests/ 引用改 tests_github/（:87 :174 :193 :231 :232 :240 :281 :450，以 grep 实测为准）——vbman-demo 属 tests_github，tests\ 方向废弃
3. 全量回归（CI）+ VBMAN demo 链路验证
4. 收尾：todo/ferock.md changelog 倒序加条、ai/C3_FIX_HANDOFF.md 已修清单更新、外壳 gitlink 同步提交（外壳 index 与子模块实际 HEAD 有偏差）、记忆双写（Memory MCP + Hindsight）

## 风险与验证

- 本机 macOS 无 MSVC：阶段 1 未做编译验证，依赖推 CI（工作分支 ferock/0.10.7）
- `__comObj` 成为 EXE 类结构体首字段 = 全量布局变更，静态论证零影响，回归确认
- CLSID/IID 走确定性哈希 fallback（EXE 不跑 TypeLib 构建/CLSID 注入，collect.inc generateClsid/generateIid 自洽）
