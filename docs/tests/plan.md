# C3 验证体系建设执行计划

> 原则：分批推进、每步独立验收、不一次性全做。方案细节见 `verify_design.md`，分层总纲见 `../自验证.md`。
> 日期：2026-09-15 ｜ 状态标记：未开始 / 进行中 / 完成 / 阻塞

| ID | 任务 | 产出 | 前置 | 工作量 | 状态 | 验收标准 |
|----|------|------|------|--------|------|----------|
| P0 | 差分冒烟首例：diff_smoke 双编译双运行对照 | tests/diff_smoke.bas + .vbp + 对照结论 | 无 | 1 小时 | 完成 | VB6Mini /make 出 exe；C3 x86 出 exe；两份 result.txt 逐行一致 |
| P1 | Test-FormRun（L1+L2）+ 8 个窗体用例 run 化 + 金样人工核对 | run_tests.ps1 +约150行 + 8 个金样 txt | 无（与 P0 独立） | 半天 | 未开始 | 全量回归 81 PASS 基线不回退；窗体用例报 run 语义；破坏金样能触发 FAIL |
| P2 | 差分脚本化：覆盖 tests 全部 .vbp 工程（补写文件反馈） | diff_runner.ps1 + 用例反馈改造 | P0 | 半天 | 未开始 | 每对工程 result 一致或差异可解释归档 |
| P3 | L3 自验证窗体 2-3 例（Form_Load 断言 + 写结果文件） | tests 新用例 | P1 | 2 小时 | 未开始 | 断言文件输出可被 run_tests.ps1 校验 |
| P4 | VBMAN 大工程差分终验（125 模块） | 终验报告 | P2 + 工程文件到位 | 1 天 | 未开始 | 双编译产物行为差异清单归档 |

## 节奏说明

- P0 先行验证方法论（最经典、最小代价）；P0 通过后 P2 才值得投入
- P1 与 P0 相互独立，可并行或按需推迟
- 每完成一项回填本表状态与验收结论

## P0 验收记录（2026-09-15）

- VB6Mini `/make`：ExitCode 0，产出 32 位 `diff_smoke.exe`（20 KB）；运行退出码 0
- C3 `--arch x86`：编译通过，产出 x86 `diff_smoke.exe`（210 KB）；运行退出码 0
- 两份 `result.txt` **逐行一致**：SUM=5050 / STR=BCDEF / FLT=12.00 / BRANCH=OK
- 覆盖语义：Long 循环累加、字符串拼接 + Chr()、Double 数组求和、Format() 浮点格式化、If 分支
- 结论：本机差分方法论（双编译 / 文件反馈谓词 / 目录隔离 / 位数对齐）端到端成立，VB6Mini 可作为差分基准编译器
