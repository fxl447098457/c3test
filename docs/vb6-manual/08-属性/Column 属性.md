# Column 属性

# Column 属性

           

**描述**

只读属性，返回 **TextStream** 文件中当前字符位置的列号。

**语法**

*object***.Column**

*object* 总是一个 **TextStream** 对象的名称。

**说明**

在写了一个新行字符后，但在写任何其他字符之前，**Column** 的值是 1。
