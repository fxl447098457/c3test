# RootFolder 属性

# RootFolder 属性

           

**描述**

返回一个 **Folder** 对象，该对象表示一个指定驱动器的根文件夹。只读属性。

**语法**

*object*.**RootFolder**

*object* 总是一个 **Drive** 对象。

**说明**

驱动器上所包含的所有文件和文件夹都可以使用返回的 **Folder** 对象进行访问。
