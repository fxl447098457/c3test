# SkipLine 方法

# SkipLine 方法

           

**描述**

当读一个 **TextStream** 文件时跳过下一行。

**语法**

*object*.**SkipLine**

*object*始终是一个 **TextStream** 对象的名字。

**说明**

跳过一行是指读取并放弃一行中的所有字符，一直到并包括该行的换行符。

如果读的文件没有打开，则产生一个错误。
