# StrReverse函数

# StrReverse函数

       

**描述**

返回一个字符串，其中一个指定子字符串的字符顺序是反向的。

**语法**

**StrReverse(***string1***)**

参数*string1*是一个字符串，它的字符顺序要被反向。如果*string1*是一个长度为零的字符串("")，则返回一个长度为零的字符串。如果*string1*为**Null**，则产生一个错误。
