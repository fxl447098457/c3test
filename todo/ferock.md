# ferock 的个人 todo

> 想法池（草稿区），约定见根目录 [todo.md](../todo.md)。决定动手的条目转仓库 Issue 后从这里删除。

## vbman 编译一致性（C3 vs 微软 VB6）

- [ ] 对照微软版 VB6，让 vbman 代码的编译结果在 C3 下与 VB6 编译结果一致，包括 VBMAN.dll 的对应执行结果
  - 基线：VB6Mini 编译 `tests\vbman\src\VBMAN.vbp` 成功（129 模块，产物 dist\DLL\VBMAN.dll 2.3MB）；C3 前端 129 模块 0 error，cl 阶段 21 错误（详见 [docs/vbman/001_first_compile.md](../docs/vbman/001_first_compile.md)）
  - 修复顺序：跨类调用实参缺失 ×15 → 事件 sink 声明 ×3 → Variant 转换散点 ×3，每修一类跑 `docs\vbman\build_vbman.ps1` 观察错误数递减
  - 编译一致后进入运行一致：VB6 与 C3 各自产出 VBMAN.dll，对比执行结果
  - 待用户后续提供 vbman 单元测试

## changelog

### 0.10.7

- 2026-09-19（src/rtl/core/vb6forms/vb6forms_uc.c → vb6forms/uc/ 6 个 .c + uc/detail/ 7 个 .inc、vb6forms_uc_internal.h、src/driver/{c3rtl.rc,rtl_embedded.hpp,rtl_embedded.cpp,driver_link.cpp}、CMakeLists.txt）：**`vb6forms_uc.c`（1546 行）按族拆为 6 个编译单元 + 7 个函数体片段，用二级子目录 `uc/` + `uc/detail/`（纯搬移零重排）** —— 主文件 1546 → 伞 57 行；`uc/` 下 `uc_host.c` 256 / `uc_hostmodel.c` 257 / `uc_host_window.c` 164 / `uc_controls.c` 157 / `uc_collection.c` 40 / `uc_debug.c` 25（最大 257）；`uc/detail/` 下 `uc_collection_api.inc` 201 / `uc_host_create.inc` 109 / `uc_debug_composite.inc` 91 / `uc_hostmodel_getprop.inc` 88 / `uc_hostmodel_call.inc` 74 / `uc_debug_dib.inc` 66 / `uc_hostmodel_setprop.inc` 32。新增 `vb6forms_uc_internal.h` 承载三类跨族符号：结构体**完整定义**（各族要解引用字段，只前置声明不够）、跨族状态、跨族函数。**踩坑 ①**：同族 `static` 提升不完整导致首次端到端 `LNK2019` ×4（`vb6_uc_registerClass` / `vb6_uc_gdiplusInit` / `vb6_uc_push` / `vb6_uc_pop`）—— 判据是「**谁调用它**」而非「函数属于哪个族」，凡调用点跨族就必须去 `static`；**踩坑 ②**：`vb6_UCDib` 原定义切给 `uc_debug.c`，而 `uc_host_window.c` 的 WM_PAINT 要 `sizeof` 与按成员访问，必须上移完整定义。RTL 5 处管线同步（rc 新增 ID 175~192、头注释 77 → **93 项** = 25 .h + 55 .c + 13 .inc；枚举、`files[]`、`driver_link.cpp` 的 `sourceFiles` 由 `vb6forms_uc.c` 换为 6 个 `uc_*.c`、CMake 93 项），顺带**补登记** `vb6rtl_userctl.h`（rc 有而 CMake 漏，属 Fix 105 遗留，四处计数由 92/93 不一致修正为 93/93/93/93）。**验证**：独立复算全过（片段 + 单元逐行一致，剩余 16 行缺口逐条核实为 banner/分节注释/交界空行等账本产物）；增量构建 0 错误；`hello.bas`(exe) 输出 `Count: 1..10` + `Sum = 5050`、退出码 0，`test_activex_dll.vbp --dll` 编译通过；清空 `%TEMP%\C3C` 后干净解包目录 **93 文件齐全、拆分相关 15 项逐字节一致（15/15）**、6 个 `uc_*.obj` 全产出；全量回归 **0 个 `REG-C3`**（`regress_all.ps1` 137 用例对照：PASS 50 / C3-EXT 36 / BOTH-FAIL 21 / SKIP-AUX 17 / COMPILE-PASS 12 / RUN-DIFF 1，`BOTH-FAIL` 全为预处理 `#Else`、`RUN-DIFF` 为 TestAXDLL 未注册的既有摆动项）。**跑回归的坑**：`regress_all.ps1` 用 `cmd /c call vcvarsall ...` 采集 MSVC 环境，本机 `cmd`/`reg.exe` 被安全策略拦 → 即使 `C3_VCVARSALL` 指向内容正确的替身 bat 也采集失败（日志头 `INCLUDE=False`），非 SKIP 用例会全报 `C1083 无法打开包括文件 stddef.h`、被误记成 6 个 `REG-C3`；**正确跑法是在 PowerShell 里先直接注入 `INCLUDE`/`LIB`/`PATH`/`VCINSTALLDIR` 再调脚本**，并用日志第 1 行的 `INCLUDE=True` 作为该轮结论有效的判据。附记：解包会话目录名是 `steady_clock` 计数，跨进程重启会复用同一数值 → 旧目录被当新会话直接覆盖，**验证解包内容前必须删干净 `%TEMP%\C3C`**，否则读到的是陈旧快照（本次曾据此误判为缓存失效，实为测量假象）

### 0.10.6

- 2026-09-19（src/backend/decl/cgen_decl_proc.cpp、src/backend/decl/cgen_decl_prop.cpp）：**Fix 123b 补齐 Sub / Property 两处形参注册链** —— Currency/Single/Date 形参同属 C double 组，上一提交（f5e72c3）只改了 `visit(FunctionDecl)`，而 `SubDecl` / `PropertyDecl` 各持一份独立复制的注册链，两处仍只注册 `Double`。于是 vbman cZipArchive 的 `frFireProgress`（**Friend Sub**，形参 Current/Total As Currency）走 Sub 链未注册 → `inferExprType(Current)` 落符号表查找（129 模块工程里 `current` 撞到同名符号）→ RaiseEvent 实参打包误走 else 的 BSTR 分支，生成 `vb6_BSTR_FromStr(double)` → cZipArchive.c(2341/2343) C2440「无法从 double 转换为 const wchar_t *」。**验证**：修复前 x86 编 VBMAN.dll exit=1（2 错）；修复后 **x86 + x64 双架构 exit=0、0 error**，产出 PE32 / 2229248 字节 与 PE32+ / 2656256 字节，生成代码改为 `__evt_args[1].vt = VT_R8; __evt_args[1].dblVal = (double)(Current);`（与 Currency→double 的 Fix 126 值语义一致）；全量回归 PASS=86 / FAIL=0 / SKIP=1 / TOTAL=87。
- 2026-09-19（src/backend/detail/expr/cgen_expr_call_pad_conv.inc → 6 个新 .inc、src/backend/expr/cgen_expr_call.cpp）：**pad_conv.inc（545 行）按函数体内既有分节注释再切 6 段（纯搬移零行为改动）** —— → `cgen_expr_call_opt_pad.inc` 81（Optional 补齐 / IsMissing `_has_` / 隐式 me）、`cgen_expr_call_conv_cstr.inc` 87（CStr 家族）、`cgen_expr_call_conv_numeric.inc` 88（CInt/CLng/CDbl/CByte/CSng/CBool）、`cgen_expr_call_conv_output.inc` 109（MsgBox / Format 首参 + CStr 兜底）、`cgen_expr_call_conv_varargs_lenb.inc` 87（CallByName 变参 + LenB 分派）、`cgen_expr_call_builtin_argtype.inc` 114（Fix 108c 内建实参适配）；原文件 `git rm`，伞文件 include 12 → 17 条。6 片段**全部自身花括号平衡**（可继续独立搬移），切分脚本对「拼回 == 拆分前逐字节一致」做断言。验证：拆分前后各对 VBMAN.vbp 跑 `--emit-c`，**109652 行逐字节一致**；全量回归 PASS=86 / FAIL=0 / SKIP=1 / TOTAL=87。
- 2026-09-19（src/driver/driver_compile.cpp、src/driver/main.cpp、src/backend/expr/cgen_expr_binary_util.cpp、src/backend/detail/util/cgen_helpers.inc、tests/test_bstr_concat_scalar.bas、tests/run_tests.ps1）：**修 bugfix 文档 260919 全部四项（B2/A-1/A-2/B3）+ 新增标量拼接用例** —— ① B2 退出码：`Driver::compile` 把「已显示帮助」与「无源文件」拆开（help/version 返 errorCount=0），`main.cpp` 的 `!success && errorCount==0` 第三层守卫同步删除（文档只写了两层，实测 `--version` 仍返 1 才发现这层）；② A-1：`wrapToBSTR` 的 `expr.find("vb6_BSTR")` 未锚定子串检查改为新增 `isBstrReturningCall()` 顶层前缀精确判定，`vb6_Len("abc")`/`vb6_Val(...)` 等带字符串实参的标量函数不再被实参里的 `vb6_BSTR_FromStr` 整体误判直通；③ A-2：前缀块兜底 `return expr; // 其他vb6_函数假定为BSTR` 删除，落到 `inferExprType(node)` —— `Err.Number` 生成 `vb6_CStrLong(vb6_ErrNumber())`（即 2026-09-18 遗留的 0xC0000005 崩溃项）、`UBound` 同理；④ B3：数值清单按 src/rtl 真实返回类型拆成 CStrDbl（Sqr/Abs/CDbl/CSng/Timer/Rnd/Fix/Int/Val，原被 CStrLong 截断 → C4244）/ CStrLong（CLng/CInt/CBool/CByte/Len/LenB/InStr/InStrRev/Sgn/VarType）两组，且 Date 检查同步改顶层函数名精确匹配（原 `find("vb6_Time")` 会把 vb6_Timer 吞进 CStrDate 分支）。**验证**：A-1/A-2 两轮 `--emit-c` 逐字节对照 99 个用例均零差异（既有用例未覆盖这些路径）；全量回归 PASS=85 / FAIL=0 / SKIP=1 / TOTAL=86（新增 `test_bstr_concat_scalar.bas` 16 项断言：Err.Number 非零 / 带与不带字符串实参 / 整数与浮点标量 / result.txt 供 VB6 原生对照，旧基线 82/0/1/83 已过时）；B2 五项退出码 `--version/-V/-h/--help/无参数` = 0、`--nosuchopt`/无源文件/语法错误 = 1。注意：本机沙箱 reg.exe 被拦导致真实 vcvarsall 拿不到 SDK 路径，改用 portable 替身 bat（`tmp/msvc_portable/vcvars.bat`，arch 感知、含 rc.exe、强制 C3 每次编译重调），`run_tests.ps1` 经 `C3_VCVARSALL` 认它
- 2026-09-19（src/backend/expr/cgen_expr_binary_util.cpp、src/backend/detail/expr/cgen_expr_call_pad_conv.inc）：**重落 7ce1f50 丢失的真修复（Fix 100a/100b/100c，取自 425f934）+ 新增 Fix 100d + 解冲突标记** —— 7ce1f50 实际提交的两个源码文件是回退版（isBstrReturningCall 只有声明无实现、Fix 118j 类型分派被还原成「其他vb6_函数假定为BSTR」直通兜底），且 `!21` 合并把 pad_conv.inc 的未解决冲突标记（<<<<<<< HEAD / >>>>>>> origin/main）直接提交进 HEAD 挡住构建（本地 main 带标记、origin/main 干净，系本地 reset+cherry-pick 链污染），当时验证用的 exe（11:34）早于提交（12:06）。本次：① 100a：wrapToBSTR:37 的未锚定 `find("vb6_BSTR")` 子串检查改为 isBstrReturningCall 顶层前缀清单判定；② 100b：直通兜底删除，落到 inferExprType(node)；③ 100c：数值清单按 RTL 返回类型拆 CStrDbl/CStrLong 两组、Date 组改顶层函数名精确匹配（vb6_Timer 不再被find("vb6_Time") 吞进 CStrDate）；④ 100d：A-1 同类第二处 :122 `find("vb6_BSTR_FromStr(")` 同步锚定 —— 修「"x=" & Asc("A")」裸传 int32_t当指针段错误（实测 exit 139），现生成 vb6_CStrLong(vb6_Asc(...)) 输出 asc=65；⑤ pad_conv.inc 取 HEAD 侧并集版 Fix 121 解掉冲突标记。**验证**：构建通过；`--emit-c` 100 用例对修复版 oracle 零差异（仅 6 处窗体样式常量 0x50000000→0x54000000来自 main 侧窗体改动与本次无关，Fix 100d 影响面快照对照 0 处变化）；全量回归 PASS=86 / FAIL=0 / SKIP=1 / TOTAL=87（test_bstr_concat_scalar PASS）；MSVC C4047/C4024/C2440/C4244 全 0；样例 len=3 err=0 sqr=1.4142135623731 val=1.5 asc=65 ub=1 退出码 0
- 2026-09-18（遗留，未修；已定位到代码位置与实测证据）：**字符串拼接里的 `Err.Number` 不生成取字符串转换 → 0xC0000005** —— `Debug.Print "p13-misserr=" & Err.Number` 生成 `vb6_BSTR_Concat(vb6_BSTR_FromStr(L"p13-misserr="), vb6_ErrNumber())`，把 `int32_t` 当 `BSTR` 指针传入 → 按地址 5 解引用即崩；同一表达式换成 `e1 = Err.Number` 再 `"..." & e1`（Long 变量）则正确生成 `vb6_CStrLong(e1)`，说明拼接路径**只在能判定出具体类型时才补转换**，而 `Err.Number` 的类型推断没落到 Long。**实测证据**：`v = c.Item("nosuchkey")`（键不存在，Collection 正确置 Err 5）后紧跟该拼接 → 进程 `0xC0000005`；改成 `CStr(e1)` 后同一用例 14/14 全对、正常退出。**排查手法**：在 RTL 临时加 `fwprintf(stderr, ...)`，确认 `vb6_ComCall` 已正常返回 NULL，把崩溃范围压到下一行拼接上。修复方向：让语义层把 `Err.Number`/`Err.Description` 等内建属性推断为具体类型，或在拼接路径对推断不出具体类型的操作数统一走 `vb6_CStr` 兜底
- 2026-09-18（合并 + src/backend/detail/expr/cgen_expr_call_pad_conv.inc、src/rtl/core/vb6forms/vb6forms_uc.c、src/rtl/core/vb6forms/vb6forms_controls.h、src/rtl/core/vb6com/vb6com_foreach.c）：**把 main 并进 `ferock/0.10.6`，并修合并后定向验证暴露的 3 处缺陷** —— 合并分两轮：先合 `f57b574`（冲突 6 处，按「main 优先 + 保留 0.10.6 独有增量」解决；RTL 内建 Collection 资源顺移让位给 main 的 UserControl 资源，改为 177/178，rc 头注释同步修正为 **24 .h + 49 .c + 6 .inc = 79**；`IndexOrCallExpr::omittedArgs` 是两侧同名同义的**真重叠**，按 main 的槽位保留表示落地，并清理被 main 取代的 0.10.6 侧 `LiteralKind::Missing` 死代码），再补合 `aa16121`（VB6 手册文档）+ `65243ff`（!18 merge vi/dev）—— 后者 480 文件 / 14967 行全在 `docs/vb6-manual/` 下，与 0.10.6 侧改动**零文件重叠**故无冲突。产物 `965e02f` + `86b6058`，已推 `origin/ferock/0.10.6`（`ls-remote` 核验 86b6058）。合并后验证查出并修 3 处：① **`CStr(裸 vb6_ComCall/ComCallByDispid/ComGetProp)` 少一层解包** —— 这三个函数返回类型是 `void*`、实际指向 Windows `VARIANT`，直接过 `_Generic vb6_VariantFromValue` 会落到 `default:` 被当 VT_DISPATCH 对象指针 → 实测 `Debug.Print "B=[" & CStr(c.Item(1)) & "]"` 得 `B=[]`，而同值走赋值路径（`s = c.Item(1)`）正常，说明只有内联拼接这一路漏了 `vb6_VariantFromComResult`；② **`vb6_Collection_EnumInit/EnumNext` 无原型声明** → `vb6com_foreach.c` 隐式按返回 `int` 处理，x64 下枚举句柄指针被截断；③ **宿主枚举句柄被当 `vb6_ForEachState` 解引用** —— `vb6_ForEach_Release` 无条件取 `state->pEnum` 调 `lpVtbl->Release`，而宿主句柄（Controls 集合 / 内建 Collection）首元素是集合指针不是 `IEnumVARIANT*` → 0xC0000005，改为先按 `vb6_UC_ControlsIsCollection` / `vb6_Collection_IsCollection` 判定、宿主句柄直接 `free`；同时把两个宿主枚举器句柄首元素由 `int32_t` 改为 `intptr_t` 存指针（`Controls` 枚举器有完全相同的截断缺陷，一并修）。**验证**：三组探针（省略实参位置 `c.Add "x", , , 1` 不错位 / For Each over Collection / 内联拼接 `CStr(c.Item(1))`）全部通过且退出码 0；全量对照回归 122 用例与合并前基线 `report-20260918-130528.csv` 逐条比对，**判定变化仅 1 例** —— `test_modulemethod.vbp` RUN-TIMEOUT→PASS（人工点掉真 VB6 侧模态框所致，见下条），其余 121 例逐条一致；本轮新增 5 例（VbQRCodegen / BalloonTooltips）系 main 带入的测试用例，不是回归
- 2026-09-18（诊断，未改代码）：**真 VB6 侧的致命错误模态框会阻塞对照回归** —— 实测 `test_onerror.bas`（真 VB6 版必弹「Run-time error '53' File not found」）与 `test_modulemethod.vbp`（必弹「424 Object required」）都会被 `regress_all.ps1` 的单例超时记成 `RUN-TIMEOUT`，**框一被人点掉就变 PASS** —— 这正是历史报告里同一用例时而 TIMEOUT 时而 PASS 的原因，与代码无关。两例的共同触发条件是**错误处理器直接掉出**（结尾没有 `Resume`/`Exit Sub`）：最小用例逐层隔离证实，单独 `On Error GoTo`、单独 `On Error Resume Next`、两个连续失败 `Open` 全程 `Resume Next` **都不弹**，只有「处理器结尾直接落到后续语句」才会让其后的运行期错误变成致命框；真 VB6 文档要求处理器前加 `Exit Sub`，C3 这点比 VB6 宽松故不弹。`test_modulemethod` 的 424 另有一层：用例调用 `MathUtils.Add(10, 20)`（标准模块的 `模块名.方法()` 限定调用）是 **C3 扩展语法**，真 VB6 在没有 `Option Explicit` 时把 `MathUtils` 当隐式 Variant，运行期抛 424；C3 版正确输出 `30` / `21`。**顺带记两条口径缺口**：`regress_all.ps1` 的 `C3-EXT` 判定只覆盖真 VB6 **编译期**失败，这类「VB6 编译期放行、运行期才炸」的 C3 扩展语法会被误记成 `RUN-DIFF`/`RUN-TIMEOUT`；另外回归跑 `.bas` 用例时 cwd 就在 `tests/`，会把 `tests/m6_test.txt`、`tests/scores.txt` 两个受版本控制的运行期输出弄脏（每次跑完需 `git checkout --` 还原，建议后续加进 `.gitignore`）
- 2026-09-18（src/ast/detail/ast_enums.hpp、src/ast/detail/ast_expr.hpp、src/ast/ast.cpp、src/semantics/semantic_analyzer_expr.cpp、src/parser/parser_expr_postfix.cpp、src/parser/stmt/parser_stmt_assign.cpp、src/backend/expr/cgen_expr.cpp、src/backend/detail/expr/cgen_expr_call_com_bind.inc、src/rtl/core/vb6com/vb6com.h、src/rtl/core/vb6com/vb6com_pack.c、src/rtl/core/vb6rtl/vb6rtl_class_com.h）：**修 COM 调用省略实参导致后续实参前移** —— `c.Add "x", , , 1`（After=1）在无括号路径被解析成 2 个实参（中间的 Key/Before 位置直接丢弃），`1` 落到第 2 个形参 Key 上 → 集合/字典的 Before/After 插入整体错位；实测表现为 `Collection.Add` 抛运行期错误 5（无效的过程调用或参数）。修复分四层：AST 增 `LiteralKind::Missing` 与 `IndexOrCallExpr::omittedArgs`；parser 无括号路径在 `canStartArg()` 失败时只登记形参位置（不往 positional 塞假值，普通函数调用的参数个数不变），带括号路径的占位由「整型 0」改为 `Missing`；cgen 的 COM 晚绑定路径改为按形参位置展开参数表，省略位置生成 `vb6_ComPackMissing()`；RTL 新增 `vb6_ComPackMissing()`（VT_ERROR + DISP_E_PARAMNOTFOUND），并在用户侧头 `vb6rtl_class_com.h` 声明 —— 缺该声明会被 C 编译器隐式按 int 返回处理，x64 下指针截断成 32 位立即访问违例，首轮实测即崩在此。**验证**：`c.Add "x", , , 1` 生成 4 个实参 `{x, Missing, Missing, 1}`；探针 `afterInsert=axbc`、`Before=1 → ya`、`Key+After` 三例全部正确且 Err=0；全量回归 PASS=82 / FAIL=0 / SKIP=1 / TOTAL=83 零回归
- 2026-09-18（新增 `src/rtl/core/vb6com/vb6com_collection.c` + `vb6com_collection_enum.c`；改 `vb6com.c`、`src/rtl/core/vb6rtl/vb6rtl_conv.c`、`src/driver/{c3rtl.rc,rtl_embedded.hpp,rtl_embedded.cpp,driver_link.cpp}`、`CMakeLists.txt`）：**新增 VB6 内建对象 `Collection` 的运行时实现** —— `New Collection` / `Dim c As New Collection` 生成 `vb6_NewObject(L"Collection")`，而 `Collection` 与 `VBA.Collection` 都不注册 ProgID（实测 `CLSIDFromProgID` 两者均 `0x800401F3`），RTL 也没有内建实现 → 只要用到集合就必然 429。改为在 RTL 侧手写 IDispatch 对象：实现 `Add`（Item/[Key]/[Before]/[After] 四个形参，含按键与按位置插入）、`Item`（默认成员 `DISPID_VALUE`，支持 `c(1)` / `c!key`）、`Count`、`Remove`、`_NewEnum`（IEnumVARIANT，接既有 `vb6com_foreach.c` 的 For Each 通路），键重复抛 457、键不存在抛 5、序号越界抛 9；错误描述复用 `vb6_StdErrorDesc`（该函数由 `static` 提升为外部链接）。`vb6_NewObject` 改为**先探内建（`vb6_NewBuiltinObject`）、未命中再回退 COM 注册表**，不影响其它类名的既有行为。RTL 5 处管线同步（rc 75 → **77 项** = 23 .h + 48 .c + 6 .inc；`RtlResourceID` 枚举、`files[]`、`driver_link.cpp` 的 `sourceFiles`、CMake `C3RTL_EMBEDDED_FILES`）。**新文件按台账「单文件 ≤500 行」口径拆成两个编译单元**（容器 + IDispatch 411 行 / 枚举器 105 行），跨文件共享的结构体与枚举器 vtable 放进既有的 `vb6com_internal.h`（未新增头文件）。**验证**：探针 14 项（创建 / Add / Count / Item 序号 / Item 键 / 默认成员 / For Each / Remove 序号 / Remove 键 / After 插入 / Before 插入 / 重复键→457 / 键不存在→5 / 释放）全部正确且退出码 0；两个新编译单元 cl `/W3` 零警告；解包目录出现两个新文件并各自产出 .obj；全量回归零回归
- 2026-09-18（src/backend/expr/cgen_expr.cpp）：**修早绑定 `New` 查表用裸模块名导致 ProgID 丢类型库前缀** —— `visit(NewExpr)` 原用 `symTab_.lookupModule(node.className)` 按**全名**查符号，而类型库解析后 coclass 按**裸名**登记（`WinHttpRequest` / `Dictionary`），源码里写的是限定名（`WinHttp.WinHttpRequest`）→ 查表必然落空 → 落到 `else` 分支把源码限定名当 ProgID 交给 `vb6_NewObject`；而 `WinHttp` 只注册了版本化名 `WinHttp.WinHttpRequest.5.1`，裸名无注册 → `CLSIDFromProgID` 返回 `0x800401F3` → 运行期 429（宿主页面报 `WinHttp.WinHttpRequest` 错误）。改为 `lookupModuleDotted`（与 `cgen_with.cpp` 的 `NewExpr` 处理、`cgen_decl_var.cpp` 的类型化 COM 登记一致），命中 `SymbolKind::Class` 后改用类型库 `ProgIDFromCLSID` 反查到的真实 ProgID。**验证**：`--emit-c` 由 `vb6_NewObject(L"WinHttp.WinHttpRequest")` 变为 `L"WinHttp.WinHttpRequest.5.1"`；修复后 x64 与 x86 产物二进制扫描裸 ProgID 残留均为 0；`cHttpServer` / `cHttpClient` / `cRedisClient` 的 `CreateInstance` + `Release` 不再报 429；全量回归零回归
- 2026-09-18（src/semantics/symbol_table.hpp、semantic_analyzer.cpp、src/driver/driver_crossmod.cpp、src/backend/module/cgen_com.cpp、src/backend/detail/util/cgen_util_dllentry_{prelude,collect,tables}.inc、src/driver/detail/driver_codegen_dll_typelib.inc、src/backend/detail/base/cgen_base_generate_{c_open,body_pass}.inc、src/backend/detail/util/cgen_helpers.inc、src/rtl/core/vb6comserver/vb6comserver{,_obj}.{h,c}）：**修 ActiveX DLL 的 Public 数据字段不进 IDispatch 表** —— 生成的 `g_vb6_disp_<cls>Methods[]` 只收 Public 方法与 Property，`Public Router As cHttpServerRouter` 这类数据字段没有表项（`L"Router"` 出现 0 次），宿主 `GetIDsOfNames("Router")` 返回 `DISP_E_UNKNOWNNAME 0x80020006` → `vb6_ComGetProp` 得 NULL → 调用方无空值检查即段错误。改为：语义阶段只收集**显式 `Public` 关键字**的数据字段（本编译器把 `Dim` 解析为 Public，而真 VB6 里 `Dim` 等价 Private，故不能按访问级判定）→ TypeLib 阶段按字段类型分配 DISPID 并回写 → 后端每个字段生成 Get(invkind 2)/Let(invkind 4) 两条共享同一 DISPID 的表项 + 取值/赋值桥接函数；RTL 新增 `vb6_ComObject_FromInstance`/`vb6_FindCoClassDesc`/`vb6_ComObject_GetInstance`，供工程内类字段按 VT_DISPATCH 回传。**验证**：vbman（129 模块 / 104 coclass）全量编译出 x64 DLL；`DllGetClassObject`+`CreateInstance` 后对 cHttpServer 的 `Router/SSE/Database/Statistics/RouteBefore/RouteAfter/LastError/MaxConnections/ScriptEngine/CrossDomain` 逐个 `GetIDsOfNames` → **10/10 成功**（修复前 0/10），DISPID 为真实值（Router=176、SSE=174…），防御段 9000+ 残留 0，`Invoke(Router, PROPERTYGET)` 返回 `vt=9`(VT_DISPATCH) 且指针非空；全量回归 PASS=82 / FAIL=0 / SKIP=1 / TOTAL=83 与基线一致
- 2026-09-18（src/backend/expr/cgen_expr.cpp）：**修 `Not <比较表达式>` 恒为真** —— `Not` 一律按位取反 `~(int32_t)(x)`，但比较运算生成的是 C 的 0/1（不是 VB6 布尔 -1/0），于是 `~1 = -2`、`~0 = -1` 两者都非零 → `Not (1=1)`、`Not x Is Nothing`、`Not TypeOf … Is …` 全部为真。改为：操作数是比较（`Eq/Neq/Lt/Gt/Le/Ge/Like/Is`）、`TypeOf` 或嵌套 `Not` 时改用逻辑取反 `(((int32_t)(x) != 0) ? 0 : -1)`，数值操作数仍走位取反。**验证**：隔离用例 `If Not o Is Nothing`（o=Nothing）与 `If Not (1=1)` 由「错误进分支」变为「正确跳过」，`Not (1=2)`、`o Is Nothing` 保持正确；全量回归 PASS=82 / FAIL=0 / SKIP=1 / TOTAL=83 零回归。**顺带解开一层**：vbman `cHttpServer.StopMe` 的 `If Not m_oServer Is Nothing Then m_oServer.CloseSck` 原本恒进分支、对未赋值的 `m_oServer` 调 `CloseSck(NULL)` → 释放实例时 0xC0000005（栈：`ComObj_Release` → `vb6_cls_cHttpServer_Destroy` → `Class_Terminate` → `StopMe` → `vb6_cTlsReMaster_CloseSck` → `vb6_cWinsock_prop_get_State`），修后该处不再触发
- 2026-09-18（已修 Fix 101，最小改动）：**内部工程类字段的 `Set X = Nothing` 走了 COM 释放路径** —— `src/backend/detail/stmt/cgen_setlet_set_prop.inc` 对「项目类实例」本应生成 `target = NULL;`（注释已写明按 `vb6_ReleaseObject` 释放会对纯 C 结构体走 vtable Release → 0xC0000005），但判定条件是 `knownClassVars_.count(targetLower)`，而 `knownClassVars_` 按**裸变量名**登记（`sse`），类模块内的目标表达式却是 `me-><字段>`（`me->sse`）→ 永不命中 → 落到 `vb6_ReleaseObject` → 把纯 C 结构体当 `IUnknown*` 解引用首成员 `__comObj`（恒 NULL）→ 崩。**实测**：x64 DLL `CreateInstance(cHttpServer)` 后 `Release` 必崩（`Class_Terminate` 第一条 `vb6_ReleaseObject(&me->SSE)`），`cRedisClient` 同样；`cCsv/cDelay/cTimer/cCollection`（Class_Initialize 不创建内部类字段）正常释放。**对照排除回归**：未含 Fix 099 的 x86 旧构建「纯 CreateInstance + Release、完全不调 Invoke」同样崩，故与 Fix 099/Fix 100 无关。**修法**（`src/backend/detail/stmt/cgen_setlet_set_prop.inc`，最小改动）：判定条件由 `knownClassVars_.count(targetLower)` 扩为「命中裸变量名 **或** `inferClassTypeOfExpr(*node.target)` 推得出项目类」，补上按**表达式类型推断**识别工程类实例这一路；`inferClassTypeOfExpr` 递归覆盖 `MeExpr` / `me->字段` / With 成员 / 方法返回类，且对 `classTypedFieldMap_` 中标为 `COM:` 的字段与 `SymbolKind::ComClass` 一律返回空串 —— 类型化 COM 仍走 `vb6_ComReleaseTyped`、其余仍走 `vb6_ReleaseObject`，不误伤；函数调用形态（`isCallTarget`）显式排除（其求值非左值，生成 `<call> = NULL` 会 C2106/C2440）。**验证**：vbman x64 DLL `CreateInstance(cHttpServer)` / `CreateInstance(cRedisClient)` 后 `Release` 不再崩（8 个工程类字段改为 `= NULL`），同工程内 2 个 COM `Dictionary` 字段仍走 `vb6_ReleaseObject` 未被误改；全量回归零回归
- 2026-09-18（src/com/typelib_parser_parse.cpp、src/com/typelib_parser.hpp）：**修早绑定对象 ProgID 丢类型库前缀** —— `parseCoClass` 里 `ProgIDFromCLSID` 反查失败时回退成**裸类名**，运行期 `CLSIDFromProgID` 必然查不到（组件未注册、或 32 位组件 + 64 位宿主进程时反查必失败，属常态路径）→ VB6 错误 429 / `0x800401F3`。改为回退到 `<TypeLib库名>.<coclass名>`（VB6 约定；实测三例全部符合：`VBMAN.dll`→`VBMANLIB`、`stdole2.tlb`→`stdole`、`msado28.tlb`→`ADODB`）；库名由 `parseTypeLib` 存入新增私有成员 `currentProjectName_`。**验证**：vbman-demo 的 Helloworld 生成代码由 `vb6_NewObject(L"cHttpServer")` 变为 `vb6_NewObject(L"VBMANLIB.cHttpServer")`、`vb6_CreateObject(L"sGlobal")` 变为 `L"VBMANLIB.sGlobal"`，裸 ProgID 残留 0
- 2026-09-18（src/backend/detail/util/cgen_util_dllentry_exports.inc）：**修 DllRegisterServer 首个 coclass 失败即整轮中止** —— 原 `if (FAILED(hr)) return hr;` 会让其后所有 coclass 全部跳过、且 `vb6_RegisterTypeLib` **永不执行**（TypeLib 完全缺失）；改为累计失败数、循环跑完、TypeLib 无条件注册，返回值仍如实反映失败（非提权调用时实测 `E_FAIL 0x80004005`，旧代码在第 1 个类就退出）。**验证**：提权调用 C3 产 `VBMAN.dll` 的 `DllRegisterServer` → `HRESULT 0x00000000`，32 位视图 `Wow6432Node\CLSID` 下 **104/104** 个 CLSID 指向该 DLL、`VBMANLIB.*` ProgID 全部就位。附更正：此前记录的「只注册 13/104」是**测量假象**（非提权运行 + 注册表视图读错），逐类 CLSID 实测格式合法且唯一，无失败项
- 2026-09-18（Fix 094 零回归复验，tests/regress_all.ps1 全量对照）：Fix 094 改的是 P24-04 的 COM 符号注册路径（影响面覆盖所有类型库），故改完重跑全量 —— 117 用例与基线 `report-20260917-235836.csv` 逐条比对**变化总数 0**，判定分布完全一致（BOTH-FAIL 10 / C3-EXT 29 / COMPILE-PASS 12 / PASS 47 / RUN-TIMEOUT 3 / SKIP-AUX 16），报告 `tests/_vb6ref/report-20260918-133707.csv`。附：Fix 094 前那轮（`report-20260918-130528.csv`）有 2 项变化，均为模态框是否被人点掉所致（`test_onerror.bas` RUN-TIMEOUT→PASS、`P6.12Client` RUN-TIMEOUT→RUN-DIFF），非代码回归
- 2026-09-18（零回归验证，tests/regress_all.ps1 全量对照）：本轮两处修复后跑 117 用例，与改动前基线 `report-20260917-235836.csv` 逐条比对，**仅 2 个用例 Verdict 变化且均非代码回归** —— `test_onerror.bas` RUN-TIMEOUT→PASS（真 VB6 侧弹「Run-time error '53'」模态框，人工点掉后不再超时；C3 侧始终 exit=0、输出 `PASS1/PASS2/PASS3a/PASS3b/Done`）、`test_com_client__P6.12Client.bas` RUN-TIMEOUT→RUN-DIFF（`TestAXDLL.Calc` 未注册，C3 侧 CreateObject 失败返回 429、VB6 侧点掉弹窗后 exit=0）；其余 115 个 Verdict 与基线**逐条一致**。附：C3 产出的 `test_activex_dll.dll`（x64）经 HKCU\Software\Classes 注册后，C3 编的 `P6.12Client.exe` 跑出 `6/6 PASSED`（exit=0），注销后残留 0
- 2026-09-18（已修，运行期冒烟实测揪出）：**C3 产物 COM 晚绑定方法调用崩 0xC0000005** —— 晚绑定宿主 `tests/vbman_host`（纯 `CreateObject("VBMANLIB.cVBMAN")` + `IDispatch`，不声明任何类型库引用）在 `tests/run_vbman_test.ps1` 下：`CreateObject` 成功（输出 `create=OK`），随即 `vm.Version()` 崩，**x86 与 x64 均复现**（`0xC0000005 ACCESS_VIOLATION`）；**对照基线**用真 VB6 产出的 `tests/vbman/dist/DLL/VBMAN.dll`（2,306,048 字节）跑同一宿主 → `PASS`、退出码 `0x00000000`、完整输出 `begin/create=OK/version=OK/create2=OK/ok=3/end`，**证明是本机环境无关的 C3 缺陷**（原因在 `IDispatch::Invoke` 的 DISPPARAMS 构造 / `Optional HostApp As Object` 缺省值填充）。**已修**（`src/backend/detail/util/cgen_util_dllentry_collect.inc`，Fix 093，两层）：① 桥接函数每个参数包 `(argc > i)` 守卫 —— 零参调用时 `args == NULL`，原代码无条件解引用 `((VARIANT*)args[0])->lVal` 必崩；`_has_<name>` 标志由恒 `1` 改为按实际实参个数。② `Vb6Type::Object` 的 ByRef 分支由 `&lVal`（`LONG*`，4 字节）改为 `(void*)&pdispVal`（指针字段的真实地址），ByVal 分支由 `lVal` 改为 `pdispVal` —— x86 下 4 字节与 `void*` 同宽侥幸可用，x64 下被当 `void**` 解引用会读 8 字节越过该字段、读到栈垃圾当 `IDispatch*`（这正是「x86 修完 PASS、x64 仍崩」的原因）。**验证**：`tests/run_vbman_test.ps1` 实测 x86 与 x64 **双双 PASS**（退出码 `0x00000000`，完整输出 `begin/create=OK/version=OK/create2=OK/ok=3/end`），注销后注册残留 0
- 2026-09-18（已修）：**GlobalNameSpace 早绑定生成未定义符号** —— `tests/test_vbman/Module1.bas` 的 `VBMAN.Version()`（`sGlobal.cls` 带 `Attribute VB_GlobalNameSpace = True`）被 C3 生成为 `vb6_BSTR_Assign(&v, vb6_VBMAN_Version());`，而 `_vb6_VBMAN_Version` 无定义 → `error LNK2019: 无法解析的外部符号`。**原因**：类型库加载失败时全程静默降级 —— `src/com/typelib_parser.cpp` 的 `loadByClsid` 两处失败分支静默 `return nullptr`，`src/driver/driver_compile.cpp` 的 `Reference=` 路径不存在时静默丢弃，P24-04 的符号注册循环被跳过，`VBMAN.Version` 落入 M22「模块名.成员」分支（`cgen_expr_member_m22_module.inc` 第 103 行）无条件拼 `vb6_<限定符>_<成员>`。**已修**（诊断补齐，最小改动，不动降级行为以免误伤合法的「模块名.方法」用法）：三处静默丢弃点各补一条 `warning VB4001`，把排查方向直接指向「类型库未注册 / 引用路径无效」。**验证**：编译 `tests/test_vbman/test_vbman.vbp` 实测输出 `(1,1): warning VB4001: TypeLib reference path not found: C:\VBMANLIB\_bin\DLL\VBMAN.dll` 与 `(1,1): warning VB4001: TypeLib not registered for CLSID/LibID: {A638B373-E3B1-4281-8BD3-957EAD54B31C}`。注：该工程 `Reference=` 指向不存在的 `C:\VBMANLIB\_bin\DLL\VBMAN.dll`（旧路径）属工程配置；类型库可用时 P24-04 正常。**Fix 094（真正的原因，`src/driver/driver_semantics.cpp`）**：P24-04 提升 GlobalNameSpace 方法时用 `size_t standardMethods = isDispatch ? 7 : 3; for (mi = standardMethods; mi < members.size(); mi++)` —— 假设「接口 members 的前 N 个是 IUnknown/IDispatch 标准方法」。该假设只对**真 VB6 生成的 TypeLib** 成立；**C3 自产的 TypeLib 接口 members 只含业务方法**（实测 `sGlobal._sGlobal` members=1 只有 `VBMAN`）→ `mi=7 < 1` 循环不执行 → 一个方法都提升不了 → `VBMAN.Version()` 退化成 `vb6_VBMAN_Version()` → LNK2019（上面的诊断只覆盖「路径不存在 / GUID 未注册」，覆盖不到这条 —— 实测 Reference 指向**存在的** C3 自产 VBMAN.dll 时同样 LNK2019）。改为**按名过滤**标准方法（`QueryInterface`/`AddRef`/`Release`/`GetTypeInfoCount`/`GetTypeInfo`/`GetIDsOfNames`/`Invoke`），真 VB6 与 C3 自产两种 TypeLib 都能正确提升。**验证**：`tmp/gns_probe/test_gns.vbp`（Reference 指向 C3 自产 VBMAN.dll）由 `vb6_VBMAN_Version()` 变为 `vb6_ComCallBSTR(vb6_ComCallObject(vb6_CreateObject(L"sGlobal"), L"VBMAN", NULL, 0), L"Version", NULL, 0)`，真实编译产出 `test_gns.exe`（177,152 字节，LNK2019 消失）
- 2026-09-18（tests/run_vbman_test.ps1、tests/vbman_host）：**新增 vbman 运行期冒烟测试**（与 `regress_vbman.ps1` 的编译期对照是两个维度）—— 流程「C3 现编 VBMAN.dll（x86/x64 分目录）→ 按架构选对应 `regsvr32`（x64 走 `System32`，32 位进程下走 `Sysnative`；x86 走 `SysWOW64`）注册 → 改写宿主 vbp 类型库引用 → 编宿主 → 跑宿主 → 断言 stdout → **finally 必注销并复核残留**」；判定矩阵 `PASS / BUILD-FAIL / ARCH-DIFF / REG-FAIL / HANG / CRASH / NO-OUTPUT / ASSERT-FAIL / UNREG-FAIL / SCRIPT-ERROR`；带 PE machine 守卫（`0x014C`/`0x8664`，拦 C3 静默降级）；`-LibDll` 可跳过编译直接测现成 DLL（跑对照基线）；零硬编码，路径走 `C3_EXE`/`C3_VBMAN_RUNROOT`/`C3_VBMAN_LIB_VBP`/`C3_VBMAN_HOST_VBP`/`C3_VCVARSALL` 或参数
  - 自提权实现注记：本机实测**提权子进程跑完不退出**（`Start-Process -Verb RunAs -Wait` 永久卡住），故父进程改认 sentinel 文件 `elevated-<token>.done.txt`（`Exit-With` 里 `Stop-Transcript` 后写），拿到后 `Stop-Process` 主动收掉；token 用时间戳保证文件名唯一，**彻底不依赖任何删除动作**（沙箱 safe-delete 守卫会静默拦 `Remove-Item`，曾导致读到陈旧 sentinel、第二轮秒退）
  - 实测结论：`ParseErrors=0`、提权链路稳定（2m33s–2m44s）、真 VB6 产物 `PASS`、C3 的 x86/x64 均 `CRASH 0xC0000005`、**两轮跑完系统注册残留均为 0**
- 2026-09-18（src/driver/driver_semantics.cpp）：修 COM get/put 同名覆盖缺陷 —— 类型库里 VB6 公开字段与读写属性是 `get_X`+`put_X` 一对同 memid 同名 FUNCDESC，而 `comMethods` 按小写名索引，后写入的 put/putref 会覆盖 getter 的返回类型（退化成 Empty）与 `isPropertyGet`，使 `ctx.Request.QueryString(...)` 被判成标量取值再拼 C 成员访问（4 个 C2224）；改为 setter 不覆盖已有 getter（读取只认 getter，写入走名字化晚绑定不查此表）。修后 `tests/vbman-demo/demos/Helloworld` 的 x86 与 x64 均编译通过（此前只有真 VB6 能编），全量回归 82/0/1/83 与基线一致
- 2026-09-18（tests/vbman-demo）：新增 vbman-demo 子模块（`git@gitcode.com:woeoio/vbman-demo.git`，master，377 文件 / 56MB，`demos/` 下 20+ 个 .vbp 示例工程）；`regress_all.ps1` 的排除模式 `.*vbman.*` 按子串匹配，会自动跳过它，无需改脚本
- 2026-09-18（src/backend/msvc_driver.*、src/driver/driver_link.cpp）：C3 的 shell-out 全部改走 CREATE_NO_WINDOW 隐藏窗口（不再弹 cmd 窗口）；rc.exe 两处调用补回外层引号，否则 cmd 剥引号破坏 rc 路径、TypeLib/版本资源静默丢失（实测 DLL 少 141KB）
- 2026-09-18（tests/regress_vbman.ps1）：新增 vbman 专项对照回归脚本（regress_all 的补集，只测 `src\VBMAN.vbp`；OleDll 用导出表比对替代运行比对；VB6 侧在副本里编译，不再弄脏子模块）
- 2026-09-17（版本起手）：分支 `ferock/0.10.6` 从 `ferock/0.10.5`（`3817c67`）起；VERSION → ` 0.10.6`（唯一权威源，行首空格 + CRLF 保持）
- 2026-09-17 修复 3 项既有缺陷（0.10.5 对照回归 `tests/regress_all.ps1` 实测揪出，全部与源码拆分无关）：
  1. **vb6_CurDir 0 参调用 C2198**（test_sysfunc）：`cgen_expr_ident_builtin.inc` 的 `zeroArgBuiltinFuncs` 路径对 `curdir` 直接拼 `vb6_CurDir()`，而 RTL 签名是 `vb6_CurDir(BSTR drive)`——ident 路径不经过 call 路径的 P14.2.2 fixup（`Dir` 之所以正常是因其不在该集合）。修复：ident 路径特判 `curdir` → `vb6_CurDir(NULL)`（RTL 的 else 分支本就健壮处理 NULL=当前驱动器）；同集合其余函数（now/date/time/command/timer 等）签名逐一核对均无此问题
  2. **vb6_vsink_http_create C2197**（test_com_events_winhttp）：`cgen_com_events.cpp` `emitComVtableSinkDecls` 生成声明 `void* vb6_vsink_<var>_create(void)`，而定义（`emitComVtableSinks`）与调用是 `void* handler` 带参——声明与定义不一致。修复：声明改为 `(void* handler)`
  3. **标准模块裸 Print LNK2019**（test_concat_leak/stress、test_p613_typelib）：C3 扩展语义接受非 GUI 工程裸 `Print`（生成 `vb6_Form_Print(NULL, ...)`），但 `driver_link.cpp` 仅在 `isGui||isDll` 时链 vb6forms 系列 → LNK2019。修复：forms 源清单抽成 `addFormsSources()`，非 GUI/DLL 时扫描生成的 .c 源码、命中 `vb6_Form_` 前缀即补链（真 VB6 拒绝该语法，判定仍属 C3-EXT，但 C3 自己生成的代码必须能链接）
- 2026-09-17 验证：构建 0 错误；5 个目标用例全过（test_sysfunc 编+跑 rc=0/0 输出正常、concat_leak/stress、p613_typelib 编+跑 rc=0/0、winhttp 编译 rc=0）；全量回归 **82/0/1/83** 与基线一致（修复零回归）

- 2026-09-17（版本起手）：分支 `ferock/0.10.5` 从 main（`66b2791`）起；按 README「版本号唯一权威源」只改根目录 `VERSION`（保持行首空格 + CRLF）→ ` 0.10.5`，其余使用方（CMake `file(READ)` / 编译期宏 / `release.bat`）自动跟随；重建后 `C3 --version` = 0.10.5。6 项静态自检全过
- 2026-09-17 源码拆分（第 3 个「纯搬移 + 函数体片段」两步法）：driver.cpp 2494→主 121 —— ① 纯搬移 9 个函数共 1713 行到 6 个新编译单元（均已登记 CMakeLists）：driver_args 232（parseArgs / compile(int,char**) / writeErrorLog / printHelp / VB6C3_VERSION_STRING 兜底宏 + printVersion）、driver_compile 384（compile(int,char**) + compile(CompileOptions)）、driver_frontend 340（runLexer / runPreprocess / runParser / runTypeLibImport）、driver_semantics 339（runSemanticAnalysis）、driver_crossmod 169（runCrossModuleResolution）、driver_link 358（runLinker）；`class ExternalRefCollector` **留原文件**（被 runCodeGeneration 内使用，须同编译单元）。② runCodeGeneration 函数体 678 行按既有 Fix/P6.x/M29 分节注释切 9 片段到 `src/driver/detail/`（prelude 5 / voidfield_scan 88 / typedfield_scan 84 / variant_funcs 70 / dup_module_vars 29 / module_loop 92 / dll_typelib 236 / dll_sync 55 / dll_entry 19），其中 dll_* 三段位于 `if (options.isDll …)` 块内（相对花括号深度 1，片段自身不闭合但拼接后逐行一致）；9 个 .inc 不登记 CMakeLists。验证：① 搬移三重断言（非空行全覆盖不重叠 / 回读比对 / 新 driver.cpp 非空行序列一致，仅空行分隔归一）；② 片段**独立复算**（用拆分前备份，不依赖拆分脚本自身断言）：9 片段去头后与原文区间逐行一致 + 伞文件结构（16 行说明 + 原文前 94 行 + 9 include + 原文尾 3 行）；③ 增量构建零错误（首轮 3 个新单元报 `C2027 使用了未定义类型 Module / SemanticAnalyzer` → 补 `ast/ast.hpp`、`semantics/semantic_analyzer.hpp`；第 2 步只重编 driver.cpp + 链接，无新增编译单元）；④ **生成代码对照：拆分前后各对 100 个样例（92 .bas + 8 .frm）跑 --emit-c，12635 行逐字节一致（100/100，diff -rq 无输出、退出码序列一致、stderr 全空）**；⑤ 回归 82/0/1/83。src 下 ≥500 行文件 2 → 1（仅剩 vb6rtl.c 5327）
- 2026-09-17（环境注记，新）：`emit_c_snapshot.sh` 开头的 `rm -rf "$OUT"` 在本沙箱被安全层截成 trash 且**失败**（`SAFE_DELETE_FAIL_CLOSED`）→ 输出目录未清空，二次采集会**追加**到旧 `_rc.txt`，表现为 `wc -l` = 200 而实际样例 100。判据改为 `awk 'NF' _rc.txt | sort -u | wc -l == 100`；比对仍以 `diff -rq` 与逐文件 `cmp` 为准
- 2026-09-17 源码拆分（最后一个 ≥500 文件，RTL）：vb6rtl.c 5327→主 418 + **13 个族编译单元**（string 489 / format 46 / conv 336 / misc 400 / system 477 / compat 413 / date 468 / array 461 / file 467 / paramarray 289 / financial 189 / com 494 / registry 186，最大 494；族区间映射 13 条写在主文件头注释），主文件保留初始化/退出 + 内存/Variant + 错误处理；`vb6_Format`（608）所在 format 族再按函数体片段切 6 个 .inc 进内层 `detail/`（extract 59 / parse 120 / numeric_pre 109 / numeric_body 138 / numeric_tail 104 / string 100，include 一律 basename——解包后平铺）。**台账原预判「32 个 static 须先造 internal.h」实测过重**：static 随唯一使用者族搬走保持 static（g_formList→system、错误栈留主），未造 internal.h；中部 #define 随使用者进族（VB6_MAX_FORMS→system、VB6_MAX_FILES→file）、setjmp.h 随错误处理留主文件。RTL 5 处管线同步：rc 56→**75 项**（23 .h + 46 .c + 6 .inc）、枚举、files[]、driver_link.cpp 的 sourceFiles（14 个 .c 逐个列举）、CMake 75 项——**RTL 的 .inc 片段必须进嵌入管线（与 C++ 侧 .inc 不登记 CMake 相反）**。验证：独立复算 14/14 + 构建 0 错误 + e2e（hello.exe / test_activex_dll --dll；RTL 批次无 --emit-c 对照可用）+ 回归 82/0/1/83；拆分前后 exe 差 ~65% 字节属 TU 布局变化（同版本两次构建仅差 2 字节 PE 时间戳）。src 下 ≥500 行文件 1 → 0
- 2026-09-17 目录整理（用户要求「别都平铺」）+ 口径修正：backend/detail 55 个平铺 .inc 按前缀收进 5 子目录（base 11 / expr 23 / module 8 / stmt 6 / util 7），167 处 include 改写；RTL format 6 片段收进 core/vb6rtl/detail/；**`.inc` 计入 ≥500 行口径**（此前漏算，发现 cgen_form_create_controls.inc 503 行）→ 再切一刀：207（内容=cgen_form.cpp 1272~1475）+ 新建 cgen_form_ctrl_style_apply.inc 300（内容=1476~1771）。验证：emit-c 快照 100/100 逐字节一致（12635 行）、167 处 include 全可解析（build 三连 0 错误）、再切独立复算通过。**最终实测：src 下 ≥500 行文件（含 .inc）= 0 个，最大 494 行**

### 0.10.4

- 2026-09-17 源码拆分（第 2 个「纯搬移 + 函数体片段」两步法）：cgen_base 1753→主 124 —— ① 纯搬移 16 个函数到 2 个新编译单元（均已登记 CMakeLists）：backend/cgen_base_type.cpp 398 行（mapType / mapComType / emitGuidInitializer / mapTypeRef / mapDeclareType / tryEvalConstInt）、backend/cgen_base_naming.cpp 225 行（defaultValue / cIdent / resolveArrayTargetIdent / lookupConstSym / isStringConstIdent / isConstIdent / constIdentType / wrapConstArgForByRef / cProcName）+ 自由函数 controlTypeFromName；主文件 1753 → 1160。② generate（原 1067 行）函数体按既有 `// ---- X` 分节注释切 11 个片段到 backend/detail/（prologue 45 / state_scan 128 / header_open 63 / crossmod 74 / c_open 117 / decl_pass 103 / evt_decl 80 / body_pass 127 / evt_impl 163 / entry 151 / epilogue 55，含头注释），主文件 1160 → 124。11 个 .inc 不登记 CMakeLists。controlTypeFromName 的两处局部前瞻声明（cgen_decl.cpp / cgen_decl_var.cpp 第 12 行）无需改动。验证：① 两步各断言「拼回原文件逐行一致」（第 2 步 13 项校验全过，用 git HEAD 原文独立复算）；② 增量构建零错误；③ **生成代码对照：拆分前用 `git archive HEAD` 导出源码树单独构建的 C3.exe（0.10.4）与拆分后各对 100 个样例（92 个 .bas + 8 个 .frm）跑 --emit-c，12635 行逐字节一致（100/100，stderr 全空、退出码序列一致）**；④ 回归 82/0/1/83。src 下 ≥500 行文件 3 → 2（仅剩 driver.cpp 2494、vb6rtl.c 5327）
- 2026-09-17（工具修复）：skill c3_giant_func_split 的 split_func_body.py 在「片段区间末行恰为空行」时落盘会丢那一行（拼回断言用内存数据，查不出来）—— 已改为写入时统一以 `\r\n` 收尾；并把该现象与「函数签名跨行时 --depth 0 扫不出候选点」写进 SKILL.md 已知坑
- 2026-09-17 源码拆分（「伞头拆分」，第 4 个头文件）：vb6forms.h 582 → 伞头 23 行 + 同目录 9 个子头（vb6forms_window.h 142 / _prop.h 77 / _prop_ctrl.h 140 / _prop_pic.h 65 / _prop_form.h 86 / _ctrlarr.h 54 / _mdi.h 48 / _webview.h 61 / _controls.h 27，均含各自头尾）。切点取文件内既有 banner（窗体框架·控件创建·Timer·消息循环·事件桥接·工具函数·Unload / 控件属性读写 / 控件数组 / MDI / WebView2 / 动态加载控件）与 204~449 那段无 banner 长块里的 `// P13.x / P17.2 / P18-F / P20-xx` 注释边界，覆盖原第 15~579 行；伞头带 BOM、子头无 BOM（与 vb6rtl_*.h 口径一致）。RTL 管线同步 4 处（.h 不进 driver.cpp 的 sourceFiles）：c3rtl.rc 新增 147~155 且头注释 47 → 56 项、RtlResourceID 枚举、files[]、CMakeLists 的 C3RTL_EMBEDDED_FILES。验证：① 9 个子头去头后与 git HEAD 版原文对应区间逐行一致（用 HEAD 原文独立复算）、拼接 == 原文件第 15~579 行；② 全量重建成功（重编 rc + rtl_embedded.cpp + driver.cpp）；③ 端到端 hello.bas(→hello.exe，运行输出 Count: 1..8、退出码 0) + test_activex_dll.vbp(--dll) 通过（回归不覆盖该路径）；④ 四处清单计数一致 56/56/56/56，解包 session 目录 56 个文件与源码树逐字节一致；⑤ 回归 82/0/1/83。src 下 ≥500 行文件 4 → 3
- 2026-09-17 源码拆分（「函数体片段」，本工程单文件最大）：cgen_expr_call 3627→主 46 + backend/detail/ 12 片段（prelude 287 / builtin_pre 321 / callee_ident 185 / callee_member 204 / callee_withm 158 / com_bind 386 / callee_params 217 / arg_emit 366 / arg_variant 317 / named_args 407 / builtin_fixup 390 / pad_conv 373，正文行数，各 +4 行头注释故最大 411）。该文件唯一顶层函数 visit(IndexOrCallExpr&) 占函数体 3611 行；切点全取既有的 `// ---- Fix/Pxxxx` 分节注释与顶层语句边界（台账原估 7~8 段，实测按语义家族切 12 段）。**arg_emit / arg_variant 两段位于 1771 行那个遍历位置实参的 for 循环内部（相对花括号深度 1）**，首次把巨型 for 循环体也切片；片段自身不闭合但拼回逐行一致。12 个 .inc 不登记 CMakeLists（非编译单元）。验证：① 12 段去头后与原文区间逐行一致、拼回 == 原文件（3627 行，独立复算）；② 增量构建只重编 1 个目标、无新增编译单元；③ **生成代码对照：拆分前后各对 100 个样例（92 个 .bas + 8 个 .frm）跑 --emit-c，12635 行逐字节一致（100/100，stderr 全空、退出码序列一致）**；④ 回归 82/0/1/83。src 下 ≥500 行文件 5 → 4
- 2026-09-17 源码拆分（首个「纯搬移 + 函数体片段」两步法）：cgen_form 2222→主 86 —— ① 纯搬移 4 个 CCodeGen 成员函数到新编译单元 backend/module/cgen_form_menu.cpp（228 行，已登记 CMakeLists）：emitMenuItem 68 / emitMenuClickDispatch 81 / escapeWideCString 46 / CCodeGen::escapeCString 15；② 余下 emitFormFramework（单函数 1945）的函数体按既有分节注释切 8 个片段到 backend/detail/（prelude 91 / ctrl_registry 117 / wndproc_subclass 336 / wndproc_create 324 / wndproc_dispatch 351 / create_controls 500 / frame_menu 165 / show 59），切点全在「相对花括号深度 0」。两个文件级 static（bytesToHexArray / 旧 escapeCString）留在原位（内部链接、调用点全在本函数内）。验证：搬移与切片各断言「拼回原文件逐行一致」；增量构建零错误；回归 82/0/1/83（8 个 .frm 编译用例全 PASS）；--dump-ast 18 样例逐字节一致 18/18；另做生成代码对照 —— 拆分前（HEAD 版）C3 与拆分后 C3 对 8 个 .frm 跑 --emit-c，1769 行逐字节一致 8/8。src 下 ≥500 行文件 6 → 5
- 2026-09-17（验证手法，新）：`--emit-c` 只把生成的 C 代码打到 stdout、不调 cl.exe，可用于「拆分前后生成代码对照」，且不受本机 reg.exe 黑名单影响；注意 `git show HEAD:<file>` 输出是 LF，写回工作区前要 `sed 's/$/\r/'` 补 CRLF
- 2026-09-17（版本号根治）：版本号收敛到 `VERSION` 唯一权威源，消除 2 处硬编码副本 —— `CMakeLists.txt` 改为 `file(READ "${CMAKE_CURRENT_LIST_DIR}/VERSION")` + `string(STRIP)` 喂给 `project()`，并加 `set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS .../VERSION)` 保证改 VERSION 会自动重配置（**缺这一行时 ninja 报 `no work to do`、宏不更新，改 VERSION 等于不生效**）；`src/driver/driver.cpp` 的 `Driver::printVersion()` 改用 CMake 经 `target_compile_definitions` 注入的编译期宏 `VB6C3_VERSION_STRING`（含 `#ifndef` 兜底）。README 第九章「版本号约定」同步改写（同步清单 → 派生清单）。验证：① `cmake -P` 单测读取逻辑（RAW ` 0.10.4\r\n` → STRIP `0.10.4`）；② 端到端传播实验 —— VERSION 临时写 9.99.9 → 构建（自动 Re-running CMake + 重编译 driver）→ `C3 --version` = 9.99.9；恢复 0.10.4 → 构建 → = 0.10.4，VERSION 字节与 git 状态干净；③ 回归 82/0/1/83
- 2026-09-17（发现，未修）：`C3 --version` 与 `-h/--help` 的**退出码是 1**，不是 0 —— driver.cpp 返回 `{opts, 0}`（注释「0 = 显示帮助后退出」），main.cpp 的 `runCompile` 见 `!result.success && result.errorCount == 0` 就 `return 1`。脚本/CI 里 `c3 --version` 会被判失败。是否改成 0 待定（属既有行为，非本次引入）

### 0.10.3

- 2026-09-17 源码拆分（函数体片段推广，600+ 行口径）：4 个单巨函数文件一次拆净 —— cgen_expr_ident 744→主 26 + detail/ 3 片段（224/190/323）、cgen_util 699→主 26 + 4 片段（64/328/229/76）、cgen_setlet 809→主 264 + 2 片段（313/247）、cgen_assign 1367→主 28 + 4 片段（397/426/230/310）；切点全部取函数体内既有的顶层语义分节注释（零重排），每例断言「head + 片段 + tail 拼回原文件逐行一致」；13 个 .inc 不需登记 CMakeLists（不是编译单元）；全量构建增量通过。src 下 ≥500 行文件 12 → 8
- 2026-09-17 回归运行器修复（tests/run_tests.ps1）：新增 `Invoke-TestExe` 统一启动出口 —— 路径 A（.NET `ProcessStartInfo` + `ReadToEndAsync` 管道捕获）优先、路径 B（`Start-Process -Redirect*`）后备，两条都失败才算 FAIL 并**打印异常原文**（旧实现 catch 只写 `FAIL (run error)`，把环境问题伪装成测试失败）；`Test-Run` / `Test-Vbp` 两处重复逻辑收敛到该函数。`.out`/`.err` 仍按原样落盘供人工复查；不改测试语义、不动 CMake。修复后 `test smoke` PASS、全量 `test all` 回到基线 `PASS=82 FAIL=0 SKIP=1 TOTAL=83`
- 2026-09-17（修正上一版归因）：把「FAIL=39 全假阴性」归因为「Start-Process 被会话安全策略阻断」**不成立** —— 复测 `Start-Process -NoNewWindow -Wait -RedirectStandardOutput` 本机可用，HEAD 版原文在相同调用方式（bat → `powershell -File`，含 bash 管道）下 3/3 通过，**无法稳定复现**；当时唯一可复现的缺陷是运行器吞异常。事件特征留档：39 个 FAIL 恰等于全部可运行 run 用例数，`.out`/`.err` 存在但 0 字节（进程没起来）
- 2026-09-17（环境注记，仍然有效）：`reg.exe` 被程序黑名单拦截 → vcvarsall 拿不到 Windows SDK 路径（`C1083 crtdbg.h` / `winsock2.h`），规避办法是把 `C3_VCVARSALL` 指向只 set `INCLUDE`/`LIB`/`PATH` 的替身 bat
- 2026-09-17 源码拆分（函数体片段 + 类体片段）：cgen_expr_member 1496→主 36 + detail/ 8 片段（precheck 119 / form_builtin 231 / obj_dispatch 190 / class_module 222 / m22_module 169 / generic_access 120 / voidptr_com 191 / class_fallback 239），首次在「相对花括号深度 1」处切分（原 173~773 行是一个 `if(IdentifierExpr)` 巨块，深度 0 只有 2 个点），片段自身不闭合但拼接后逐行一致；ast_printer 581→主 47 + ast/detail/ 5 个类体片段（entry 13 / decl 93 / stmt 222 / expr 93 / support 128），在 `class PrintVisitor` 类体内 `#include`（继 cgen.hpp 之后第二次用类体片段）。拆前存 `--dump-ast` 基线（18 样例 / 464 行），拆后逐字节一致 18/18；增量构建零错误；回归 82/0/1/83。src 下 ≥500 行文件 8 → 6
- 2026-09-17（环境注记，x86 路径）：`--arch x86` 时 C3 内部会 `call vcvarsall.bat x86`（msvc_driver_discovery.cpp 的 buildVcvarsPrefix），而 reg.exe 被拦 → `C1083 windows.h`，表现为回归里恰好 2 个 `FAIL (compile)`（test_earlybound2 / test_not_com，run_tests.ps1 里唯一的 -Arch x86 用例）—— 不是代码回归。规避：把 `VCINSTALLDIR` 指向伪 VS 目录 `%TEMP%\c3_fake_vc`，其下放按 %1 切 x64/x86 的 `Auxiliary\Build\vcvarsall.bat` 替身；配 `C3_VCVARSALL=%TEMP%\wb_vcvarsall_fake.bat` 后回归回到 82/0/1/83

### 0.10.2

- 2026-09-17 源码拆分（生成物）：vb6_di_win32_stubs.c 1184→di/ 下 7 个按 Lib 家族生成的桩文件（最大 377 行）+ 手写 vb6_di_stubs.c 204；先改 cgen_decl_api.cpp 写 vb6_di_lib 标记，再改 gen_di_stubs.ps1 摆脱 unresolved_syms.txt 并支持重跑；RTL 管线 41→47，重建 99/99 + e2e 2/2 + 回归 82/0/1/83。
- 2026-09-17（版本）：三处版本号统一为 0.10.2 —— VERSION / driver.cpp 的 `C3 version` / CMakeLists.txt 的 `project(... VERSION ...)`（此前 0.10.1 / 0.10.0 / 0.1.0 三处不一致）
- 2026-09-17 源码拆分（纯搬移）：semantic_analyzer.cpp 666→主 317 + dispatch 95 + register 177 + typeref 114，分派辅助 / 注册声明（Pass1）/ 类型引用解析各成一家；保留行 + 三区间拼回与原文件一致（仅丢 2 处多余空行）；重建 99/99 + e2e 2/2 + 回归 82/0/1/83。src 下 ≥500 行文件 14 → 13
- 2026-09-17 源码拆分（函数级抽取首例）：semantic_analyzer_builtin.cpp 795（registerBuiltins 单函数 780）→ 26 行伞文件 + src/semantics/builtin/ 3 个「函数体片段」（consts 289 / consts_ext 211 / funcs 295），片段在函数体内 #include、局部 lambda 与 kVariantArray 原样不动 → 零重构零行为改动；三片段拼回与原文件逐行一致；重建 96/96 + e2e 2/2 + 回归 82/0/1/83。src 下 ≥500 行文件 15 → 14
- 2026-09-17 源码拆分（RTL）：vb6forms_picture.c 528→275+274、vb6forms_widget.c 502→231+291，主文件逐字节不动；RTL 5 处管线全同步（rc 139/140 + 枚举 + files[] + driver sourceFiles + CMake），四处清单计数核对 41 一致；重建 96/96 + e2e 2/2 + 回归 82/0/1/83。src 下 ≥500 行文件 17 → 15
- 2026-09-17 源码拆分（续）：再拆 5 个「单函数主导但主导函数 <500 行」的文件 —— cgen_com 682→257+439、parser_decl 655→274+390、parser_expr 590→402+199、typelib_builder 527→345+203、frm_parser 516→320+209；主文件逐字节不动，5 个新文件登记 CMakeLists；重建 96/96 + 回归 82/0/1/83 零变化。src 下 ≥500 行文件 26 → 17
- 2026-09-17 源码拆分（② 组）：4 个「单函数主导但主导函数 <500 行」的文件纯搬移拆净 —— cgen_localdecl 552→375+191、cgen_decl_proc 552→256+309、semantic_analyzer_decl 551→387+178、cgen_expr_binary 514→356+172；主文件逐字节不动，4 个新文件登记 CMakeLists；重建 91/91 + 回归 82/0/1/83 零变化。同时修正台账口径：单函数主导 ≠ 必须函数级抽取
- 2026-09-17 cgen 头拆分：cgen.hpp（996 行单类头）拆为伞头 54 行 + cgen_emitter.hpp（CodeEmitter 独立成头）+ detail/ 3 个类体片段（api 203 / state 342 / helpers 382），类内 #include 片段、逐行零重排，36 处引用路径不变；回归 82/0/1/83 零变化
- 2026-09-17 AST 头拆分：ast.hpp（1459 行）拆为伞头 18 行 + src/ast/detail/ 8 子头（enums/fwd/base/expr/stmt/stmt_io/decl/util，最大 366 行），按类边界切、零重排，6 处引用路径不变；回归 82/0/1/83 零变化
- 2026-09-17（ai/003-开发计划）：补「实现说明」——P4~P8 的 rtl/*.hpp/cpp 规划名是早期 C++ 路线，实际走 C 侧 RTL，相关文件名已不存在；原计划文本不改写
- 2026-09-17（src/rtl/core）：41 个平铺文件按家族分 4 个子目录（vb6rtl 9 / vb6com 8 / vb6comserver 7 / vb6forms 13），根下只留 2 个 DI 桩 + bstring；同步 rc / CMake 路径，CMake 嵌入依赖由 17 项补齐为 39 项
- 2026-09-17 RTL 头拆分：vb6rtl.h（1007 行）拆为伞头 16 行 + 7 子头（base/bstr/variant/builtin/array/class_com/runtime），同步 4 处嵌入管线，回归 82/0/1/83 零变化

###  0.10.1
- 2026-09-17 清理：删 20 个纯占位空文件；同步 ai 文档旧路径；更新外壳 README / CLAUDE
- 2026-09-16 源码拆分专题：巨型文件按家族拆到约 500 行/文件（RTL 3 个 + C++ 6 个，另含 backend 目录重组）
- 2026-09-16 Fix 099（parser_expr.cpp）：归因过程中最大反转——诊断打印 0 命中证明问题根本不在 cgen，而在 parser：With 块内 Users.Decode .Rs 被误解析成链式成员访问而非“实参 .Rs”。Fix 077 的空格检测扩展到 obj.Method .Field 形态。-2
- 2026-09-16 Fix 098（cgen_stmt.cpp）：Set 左值是函数调用表达式 → 求值后丢弃（VB6 语义 no-op）。-2
- 2026-09-16 Fix 097（type_system.cpp + cgen_expr.cpp）：CompareMethod（VBA 枚举的无 vb 前缀别名）两处统一映射 Long。-1
- 2026-09-16 Fix 096（cgen_com.cpp）：WithEvents 未实现的事件生成空 stub，vtable 不再悬空引用。-3
- 2026-09-16 Fix 095（driver.cpp）：撞名时标准模块过程优先于类成员——mWebSocketUtils.bas 与 cWebSocketUtils.cls 双定义，裸调该解析到模块版（无 me 参数）。-13，一处修复清掉 WebSocket 全家
- 2026-09-14~15 工程化：脚本 vswhere + 环境变量可移植；顶层目录英文化；README / CONTRIBUTING / ENV；冒烟测试；VERSION
- 2026-09-11 M31-M33：链接期 LNK1104 / LNK2019 / LNK2005 全部清零，VBMAN.dll 可注册（tag v0.1.0-m33-vbman-dll）
- 2026-09-02 编译提速：/MP 并行 + --no-warn + --incremental + --trim-includes
- 2026-09-01~10 vbman 编译期错误清零：777 → 0，全程 bisect 子集跟踪
- 2026-08-31 x64：LongPtr / LongLong 类型 + 指针安全运行时
- 2026-07-18 M29/M30：COM DLL DISPID 不一致（Calc.Add 找不到成员）；x86 交叉编译路径拼接
- 2026-07-14 M27/M28：第三方 COM 事件 SourceIID；VB6 字符串语义（vbCrLf / 双引号转义）+ Format 约 600 行重写
- 2026-07-06~10 P24/P25：COM 后期绑定 + 集合枚举 + 服务质量；frxParse 端到端跑通
- 2026-07-02~05 M12/M13：FRX 二进制资源读取；双架构 x64/x86；COM ProgID / Variant 数组索引 / 定长字符串系列修复
- 2026-07-01 M22：首个真实 VB6 工程（realVBP + 窗体 demo）编译链接成功，9 项运行时修复
- 2026-06-30 P17-P21：兼容性填平（Batch A-F）+ 74/74 回归零失败基线，M14
- 2026-06-29 P14-P16：兼容性深化 + WithEvents 控件事件 + ActiveX DLL 能力验证，M11/M12
- 2026-06-27 P8-P10：TypeLib 内建生成替代 MIDL；RTL 改以 RC 资源内嵌（零配置编译），M9/M10
- 2026-06-26 P5-P7：多模块工程 / COM 对象模型 / 窗体控件三项综合验证，M6-M8
- 2026-06-25 P2-P4：两遍语义 + 120+ 内置符号；C 代码生成 + MSVC 驱动，编译出 hello.exe，M3-M5
- 2026-06-24 P0-P1：词法器（LL(2)+Unicode）+ 解析器（约 2900 行）跑通，M1/M2
