# 07 - P7 窗体+控件 开发历程

> 阶段: P7 窗体+控件
> 日期: 2026-06-26
> 状态: P7.1-P7.4已完成, P7.5待开始

---

## 1. 目标

为 VB6 编译器(c3.exe)添加 Win32 窗体和控件支持，使 .frm 窗体文件能编译为原生 Windows 可执行程序。

---

## 2. 过程

### P7.1 .frm/.frx 文件解析 (已完成)

- 实现 `frm_parser.hpp`(195行) + `frm_parser.cpp`(441行)
- 支持 20+ 控件类型枚举（CommandButton, TextBox, Label, CheckBox, OptionButton, Frame, ListBox, ComboBox, Timer, Image, PictureBox, HScrollBar, VScrollBar, DriveListBox, DirListBox, FileListBox, Data, OLE, Line, Shape, Menu）
- `controlTypeToWin32Class()` 将 VB6 控件类型映射为 Win32 窗口类名（BUTTON, EDIT, STATIC, LISTBOX, COMBOBOX）
- `--dump-frm` CLI 选项输出解析结果
- 两个测试文件（Form1.frm + empty_form.frm）解析验证正确
- FrmFormDesc 数据结构：包含窗体名、位置大小、控件列表（每个控件含类型/名称/属性映射）
- FrmValueType 支持 String/Int/Float/Bool 四种属性值类型

### P7.2 Module 集成 isFormModule (已完成)

- Module 新增 `isFormModule` 标志和 `frmFiles_` 存储
- Driver 检测 .frm 文件 → 设置 isFormModule + 提取 VB 代码区 + FrmFormDesc* 传递给 cgen
- .frm 文件的 Attribute VB_Name 行作为模块名
- 语义分析 `semantic_analyzer.cpp` 增加 .frm 模块处理（从代码区提取过程声明，跳过窗体描述部分）
- Driver `driver.cpp` 增加 .frm 文件识别与加载流程（~95行新增/修改）

### P7.3 Win32 窗体代码生成 (已完成)

**提交**: `4a5719d` feat(cgen): 添加窗体模块Win32框架代码生成及Property前缀支持
**修复提交**: `55bb78b` fix(cgen): 修复VARIANT重定义冲突及空窗体WinMain生成

#### 第一轮实现 (4a5719d)

- 实现 `vb6forms.h`(107行) + `vb6forms.c`(204行) Win32 运行时
  - `vb6_RegisterFormClass` — WNDCLASSEXA 注册
  - `vb6_CreateFormWindow` — CreateWindowExA 创建主窗口
  - `vb6_CreateControl` — 创建子控件（BUTTON/EDIT/STATIC等）
  - `vb6_TwipToX/Y` — 缇到像素转换（1缇=1/15像素）
  - `vb6_ShowForm` — 显示窗体（支持模态/非模态）
  - `vb6_UnloadForm` — 卸载窗体
  - `vb6_MessageLoop` — 标准消息循环
  - `vb6_NextControlId` / `vb6_ResetControlId` — 控件ID分配
  - `vb6_SetFormUserData` / `vb6_GetFormUserData` — GWLP_USERDATA 桥接
  - `vb6_GetAppInstance` / `vb6_SetAppInstance` — 全局实例句柄
- cgen 新增 `emitFormFramework()` 生成（~482行新增）：
  - WndProc（WM_CREATE/WM_COMMAND/WM_CLOSE/WM_DESTROY）
  - CreateControls 函数（遍历控件生成 vb6_CreateControl 调用）
  - Show 函数（注册窗口类+创建窗口+显示）
  - WinMain 入口点（vb6_Init → Show → MessageLoop → vb6_Exit）
- MSVC 驱动新增 GUI 模式检测：`isGui` 标志 + `/SUBSYSTEM:WINDOWS` + `user32.lib gdi32.lib ole32.lib oleaut32.lib uuid.lib`
- CMakeLists.txt 新增 vb6forms.c 编译目标

#### 关键修复 (55bb78b)

- **VARIANT 重定义冲突**: vb6rtl.h 定义的结构体 `VARIANT` 与 `windows.h` 中的 Windows VARIANT 冲突。重命名为 `vb6_VARIANT`，全局替换（vb6rtl.h + vb6rtl.c + cgen 代码生成），同时调整 `windows.h` 包含顺序
- **空窗体 WinMain 缺失**: 无过程的 .frm 模块（如 empty_form.frm）不生成任何过程，导致 WinMain 未生成。修复：窗体模块始终生成 WinMain 入口点
- **硬编码 Win32 常量**: `emitFormFramework` 使用硬编码数值替代 `WS_CHILD`/`WS_VISIBLE` 等宏，避免生成代码依赖 `windows.h`
- **WM_CREATE 大括号不匹配**: 代码生成缩进错误导致 WM_CREATE 分支缺少闭合大括号
- **Form_Load 条件调用**: 添加 Form_Load 存在性检查后调用
- **CMakeLists.txt 零宽字符污染**: 被内置 write 工具注入约10.5KB零宽字符，导致 CMake 解析失败。需删除 .build 目录重建，并用 text-writer 重写 CMakeLists.txt

#### 端到端验证 (P7.3)

- `empty_form.frm` 编译链接成功，生成 ~251KB empty_form.exe
- 窗口正常显示并运行（3秒后关闭验证）
- 48 回归测试全通过，零回归
- 构建脚本修复：`build_clean.bat` + `strip_zw.bat`(零宽字符剥离) 辅助解决污染问题

### P7.4 消息循环+DoEvents (已完成)

本阶段共修复 5 个问题：

#### 问题1: DoEventsStmt/BeepStmt 代码生成为 TODO 注释

- **根因**: `emitStmtList()` switch 中缺少 `BeepStmt` 和 `DoEventsStmt` case 分支，落入 default 产生 `/* TODO */`
- **修复**: 添加两个 case 到 emitStmtList：
  ```cpp
  case ASTNodeKind::BeepStmt:     visit(static_cast<BeepStmt&>(*stmt)); break;
  case ASTNodeKind::DoEventsStmt: visit(static_cast<DoEventsStmt&>(*stmt)); break;
  ```
- visit(DoEventsStmt) → `vb6_DoEvents()`，visit(BeepStmt) → `vb6_Beep()`
- 同时在 `builtinFuncs` 映射表和 `zeroArgBuiltinFuncs` 集合中添加 `doevents`/`beep`

#### 问题2: Form_Load/Form_Unload 符号查找失败

- **根因**: `symTab_.lookup(formLoadFn)` 用 C 函数名 `"vb6_Form_Load"` 查找，但符号表存的是 VB6 名 `"Form_Load"`
- **修复**: 改用 VB6 名查找：`symTab_.lookup("Form_Load")` / `symTab_.lookup("Form_Unload")`
- 影响：导致 WM_CREATE 中 Form_Load 不被调用、WM_CLOSE 中 Form_Unload 不被调用

#### 问题3: Form_Unload 回调签名不匹配

- **根因**: vb6forms.c 回调类型 `int (*)(void)` 与 VB6 的 `void Form_Unload(int16_t* Cancel)` 签名不兼容
- **修复**: 在 WndProc 之前生成 trampoline 桥接函数：
  ```c
  static int vb6_Form_Unload_trampoline(void) {
      int16_t vb6_cancel = 0;
      extern void vb6_Form_Unload(int16_t*);
      vb6_Form_Unload(&vb6_cancel);
      return (int)vb6_cancel;
  }
  ```
  WM_CLOSE 中使用 trampoline 函数指针而非直接 VB6 函数指针

#### 问题4: vb6_Beep 链接错误

- **根因**: `MessageBeep()` 在 `user32.dll` 中，但 vb6rtl.c 未包含 `windows.h`，且控制台链接缺少 `user32.lib`
- **修复**:
  - vb6rtl.c 添加 `#include <windows.h>`（在 `#ifdef _WIN32` 块内）
  - msvc_driver.cpp 所有三种链接配置（DLL/GUI/Console）均添加 `user32.lib`
- 此修复同时解决了回归测试中 15/48 个失败（所有编译测试因 VB6RTL_DIR 缺失+链接错误而失败）

#### 问题5: vb6forms.c C4133 警告 + 测试脚本 VB6RTL_DIR 缺失

- **修复**:
  - `LoadCursorA(NULL, IDC_ARROW)` → `LoadCursorA(NULL, (LPCSTR)IDC_ARROW)` 类型转换
  - `LoadIconA(NULL, IDI_APPLICATION)` → `LoadIconA(NULL, (LPCSTR)IDI_APPLICATION)` 类型转换
  - `LoadIconA((HINSTANCE)hInstance, MAKEINTRESOURCEA(iconResId))` → 添加 `(LPCSTR)` 转换
  - `run_tests.ps1` 添加 `$env:VB6RTL_DIR = "D:\vb6pro\src\rtl\core"` 在 MSVC 环境加载之后
- VB6RTL_DIR 缺失原因：c3.exe RTL 查找优先读 `VB6RTL_DIR` 环境变量，未设置时从 CWD 向上搜索 `src/rtl/core`，但测试脚本不在此工作目录运行

#### 端到端验证 (P7.4)

- `form_test_p74.frm` 编译链接成功，生成 255KB `form_test_p74.exe`（零编译警告）
- 窗口正常显示，消息循环工作，5秒后强制关闭
- 生成代码验证：`vb6_DoEvents()`、`vb6_Beep()`、`Form_Load()`、`Form_Unload_trampoline()` 均正确
- 测试窗体包含：cmdDoEvents(CommandButton)、cmdBeep(CommandButton)、lblInfo(Label)
- 事件代码：DoEvents 5次循环、Beep 调用、Form_Load 初始化变量、Form_Unload(Cancel=0)
- 48 回归测试全通过，零回归

#### 脚本工具化 (P7.4 收尾)

将常用命令操作封装为脚本，避免每次会话手动输入：
- `scripts/build.bat [clean]` — 构建 c3.exe（自动加载 vcvarsall）
- `scripts/test.bat [all|run|compile|syntax] [verbose]` — 运行回归测试（自动设置 VB6RTL_DIR）
- `scripts/compile.bat <source> [outdir]` — 编译 .bas/.frm/.vbp（自动加载 MSVC+VB6RTL）
- `scripts/run.bat <exename> [timeout]` — 运行 output/ 下的 EXE
- `scripts/dev.ps1` — PowerShell 一键构建+测试（Agent 会话用）
- `scripts/env.ps1` — 加载 MSVC 环境（dot-source 方式）
- `scripts/compile_form.ps1 <form.frm> [-Run]` — 编译窗体+可选运行
- ENV.md 更新：添加脚本使用说明和当前阶段状态

---

## 3. 验证

| 验证项 | 结果 | 备注 |
|--------|------|------|
| 48 回归测试 | 48/48 PASS | 零失败 |
| empty_form.frm 编译运行 | 通过 | 空窗体正常显示 |
| form_test_p74.frm 编译运行 | 通过 | DoEvents+Beep+Form_Load+Form_Unload 窗体 |
| c3.exe 构建 | 零错误 | msvc_driver.cpp + cgen.cpp 变更 |
| vb6forms.c 编译 | 零警告 | C4133 已修复 |
| vb6rtl.c 编译 | 零警告 | MessageBeep 声明来自 windows.h |
| scripts/build.bat | 验证通过 | 增量构建和 clean 构建均正常 |
| scripts/test.bat | 验证通过 | run 模式 13/13 PASS |
| scripts/compile.bat | 验证通过 | .bas 和 .frm 编译均正常 |

---

## 4. 问题与决策

| 问题 | 决策 | 原因 |
|------|------|------|
| 控件属性访问(txtOutput.Text等)生成C结构体访问 | 列入P7.5 | 需要映射VB6属性到Win32 API(GetWindowText/SetWindowText等) |
| MSVC环境变量不跨PowerShell会话持久 | 创建env.ps1 + compile脚本自动加载 | 手动测试时需每次重新加载，封装脚本可复用 |
| Form_Unload回调签名不匹配 | 生成trampoline桥接 | C语言无法直接将void(int16_t*)转为int(*)(void) |
| VB6RTL_DIR环境变量 | 添加到测试脚本+compile脚本 | c3.exe RTL查找优先环境变量，缺失时从CWD向上搜索不可靠 |
| VARIANT重定义冲突 | 重命名为vb6_VARIANT | 避免VB6自定义VARIANT与Windows SDK VARIANT冲突 |
| 硬编码Win32常量 | emitFormFramework使用硬编码数值 | 生成代码不依赖windows.h，只要vb6rtl.h含windows.h即可 |
| CMakeLists.txt零宽字符污染 | 用text-writer重写+strip_zw.bat辅助 | 内置write工具注入~10.5KB零宽字符，需彻底重写 |
| cgen.hpp被意外清空 | git checkout 恢复 | PowerShell变量作用域问题导致WriteAllText写入空内容 |

---

## 5. 修改文件清单

### P7.1-P7.3 阶段

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| src/project/frm_parser.hpp | 新增 | .frm解析器头文件(195行)，FrmFormDesc/FrmControl/FrmControlType/FrmValueType定义 |
| src/project/frm_parser.cpp | 新增 | .frm解析器实现(441行)，VB.Form/VB.Control解析+属性值提取+控件类型枚举映射 |
| src/backend/cgen.hpp | 修改 | 新增emitFormFramework()声明，Property前缀prop_get_/prop_let_/prop_set_ |
| src/backend/cgen.cpp | 修改 | emitFormFramework()实现(~482行)，WndProc+CreateControls+Show+WinMain生成 |
| src/backend/msvc_driver.cpp | 修改 | isGui检测+/SUBSYSTEM:WINDOWS+user32.lib+gdi32.lib |
| src/backend/msvc_driver.hpp | 修改 | MsvcOptions新增isGui字段 |
| src/driver/driver.cpp | 修改 | .frm检测+vb6FormModule处理+FrmParser集成+frmDescs_传递(~95行) |
| src/driver/driver.hpp | 修改 | frmDescs_/frmFiles_存储字段 |
| src/rtl/core/vb6forms.h | 新增 | Win32窗体运行时API(107行) |
| src/rtl/core/vb6forms.c | 新增 | Win32窗体运行时实现(204行) |
| src/rtl/core/vb6rtl.h | 修改 | VARIANT→vb6_VARIANT重命名 |
| src/rtl/core/vb6rtl.c | 修改 | VARIANT→vb6_VARIANT重命名+windows.h包含顺序调整 |
| src/semantics/semantic_analyzer.cpp | 修改 | .frm模块代码区过程声明处理 |
| src/semantics/symbol_table.hpp | 修改 | 符号表支持窗体模块符号 |
| src/semantics/symbol_table.cpp | 修改 | 窗体模块符号注册 |
| CMakeLists.txt | 修改 | 新增vb6forms.c编译+vb6c3-cgen库 |
| tests/test_form/Form1.frm | 新增 | 窗体解析测试文件 |
| tests/test_form/empty_form.frm | 新增 | 空窗体端到端测试 |
| ENV.md | 新增 | 开发环境速查文档 |

### P7.4 阶段

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| src/backend/cgen.cpp | 修改 | emitStmtList添加BeepStmt/DoEventsStmt case; Form_Load/Form_Unload符号查找修复(lookup VB6名); Form_Unload trampoline桥接函数生成; cgen.hpp意外清空恢复 |
| src/backend/msvc_driver.cpp | 修改 | 所有链接配置(DLL/GUI/Console)添加user32.lib |
| src/rtl/core/vb6rtl.c | 修改 | 添加`#include <windows.h>`(解决MessageBeep C4013警告+LNK2019) |
| src/rtl/core/vb6rtl.h | 修改 | 新增vb6_Beep/vb6_End声明 |
| src/rtl/core/vb6forms.c | 修改 | C4133修复(LPCSTR类型转换); 模态窗体支持; Timer管理; DoEvents安全限制; Form_Unload回调机制 |
| src/rtl/core/vb6forms.h | 修改 | Timer/Form_Unload回调API声明; DoEvents返回值类型 |
| tests/run_tests.ps1 | 修改 | 添加VB6RTL_DIR环境变量设置 |
| tests/test_form/form_test_p74.frm | 新增 | P7.4测试窗体(DoEvents+Beep+Form_Load+Form_Unload) |
| scripts/build.bat | 新增 | 构建脚本(自动vcvarsall) |
| scripts/test.bat | 新增 | 测试脚本(自动VB6RTL_DIR) |
| scripts/compile.bat | 新增 | 编译脚本(自动MSVC+VB6RTL) |
| scripts/run.bat | 新增 | EXE运行脚本 |
| scripts/dev.ps1 | 新增 | PowerShell一键构建+测试 |
| scripts/env.ps1 | 新增 | MSVC环境加载脚本 |
| scripts/compile_form.ps1 | 新增 | 窗体编译+运行脚本 |

---

## 6. 状态

**P7.4 已完成**。下一步 P7.5 内置控件集：实现控件属性访问（Text/Caption/Value 等 → Win32 API 映射），使 VB6 代码能读写控件属性。
