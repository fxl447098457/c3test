# VB6 二进制资源 (窗体属性侧车) 一律不得做行尾/编码转换。
#
# .frx/.ctx/.pgx 是 VB6 保存窗体时生成的二进制容器 (.frm/.ctl/.pag 的同名侧车),
# 内部是 [长度前缀][数据] 的紧凑结构。里面的偏移量按**字节**记账, 只要 git 把
# 某个 CRLF 改成 LF, 所有后续偏移全错, 解析出的是垃圾。
#
# 风险来源: core.autocrlf=true 时 git 靠"前 8KB 里有没有 NUL 字节"来判定文本;
# 这些小文件 (几十字节到几 KB) 一旦被判成文本就会被改写。实测当前仓库里的
# .frx 恰好都含 NUL 所以没被改坏, 但那是**巧合**, 且本机 autocrlf=true 与
# Linux CI 的 autocrlf=false 行为不一致 —— 显式标 binary 把这个不确定性消除。
#
# `binary` 是 git 内置宏 = -diff -merge -text, 对已按二进制存储的既有文件是
# no-op (不会引起任何既有 blob 重写), 纯属把判定钉死。
*.frx binary
*.ctx binary
*.pgx binary

# ai/028 V1: 反引号原始串的行界/编码变体夹具 —— **字节本身就是被测对象**。
# core.autocrlf=true 会把 *.bas 统统按 LF 存进索引 (实测 i/lf)，于是 checkout 时
# 存成 CRLF 的那两份与存成 LF 的那两份在 CI 上变成同一份文件，"同一内容四种源
# 读回同一个串"这条判据当场静默退化成两轴里的一轴 (上面 .frx 那段说的就是这种
# 本机/CI 不一致)。`-text` 关掉两个方向的转换 ⇒ 工作区字节 == blob 字节。
tests/rawstr_var/*.bas -text
tests/rawstr_neg/*.bas -text
tests/rawstr_proj/* -text
tests/test_rawstr.bas -text
# ai/028 V2 同理: 插值用例的**行号/列号**就是断言对象 (负例 n4 钉"第二个孔落在原文件
# 第 8 行"), 行尾被改写就会挪位置。
tests/interp_neg/*.bas -text
tests/test_interp.bas -text
